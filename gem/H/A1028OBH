作者: itoc (祝妳星星快樂 :)) 看板: plan
標題: [文件] 一些常用參數的名稱
時間: Sun Sep  8 21:03:48 2002

  一些常用的變數名稱

  FILE *fp;             // file pointer
  int fd;               // file descriptor

  int ch;               // vkey/vand char
  int num;              // number
  int pos;              // position
  int max;              // maximum
  usint ufo;            // user favor option

  char buf[80];         // buffer
  char msg[80];         // messsage
  char tmp[80];         // temporary
  char cmd[128];        // command
  char ans[5];          // vmsg answer
  char uid[IDLEN + 1];  // user id

  char fpath[64];       // file path
  char folder[64];      // folder path

  char *str;            // string
  char *ptr;            // pointer
  char *dir;            // directory

  screenline slp[2];    // screenline pointer
  screenline slt[T_LINES]; // screenline total

  HDR hdr;              // header (檔頭)
  HDR mhdr, fhdr, ghdr; // mail/file/gem header

  BRD brd;              // board header
  MF mf;                // my favorite
  NBRD nbrd;            // new board

  ACCT acct;            // account
  ACCT *u;              // user
  ACCT cuser;           // current user
  UTMP utmp, *up;       // user temporary
  UTMP cutmp;           // current user temporary

  PAL pal;              // pal
  ALOHA aloha;          // aloha (==hello)

  BMW bmw;              // bbs message write
  BENZ benz;            // 有 bmw 當然有 benz :p (系統協尋)

  XO *xo;               // overall (xover) interactive routines
  XO *xt;               // xover temporary
  XZ xz;                // xover zone

--
[1;37m□ 本文章由 [33mitoc[37m 從 [32mitoc.Dorm-GD2.NCTU.edu.tw[37m 發表[m
